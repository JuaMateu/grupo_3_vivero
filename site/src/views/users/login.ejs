<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head') %>
    <title>Login</title>
  </head>
  <body>
    <%- include('../partials/header') %>
    <!-- Aca va el cuerpo la pagina -->
    <main>
        <section class="flex-column media-flex-row">
            <div class="auto-flex">
                <!-- Aqui va el texto principal de la pagina -->
                <h1 class="users__title">Iniciar Sesión</h1>
                <!-- Seccion de pregunta de sign up -->
                <div class="users__subtitle">
                    <h4>¿Aún no tenés una cuenta?</h4>
                    <a href="/users/register" class="users__link">Creá una cuenta</a>
                </div>
            </div>
            <div class="auto-flex">
                <!-- Iniciamos el formulario de login -->
                <form action="/users/login" method="post" class="users-form">
                    <!-- Ingresar el correo electrónico -->
                    <div class="users-form__field">
                        <label class="users-form__label" for="email"> Correo electrónico</label>
                        <input 
                        class="users-form__input" 
                        name="email" 
                        type="email" 
                        placeholder="Ingresá tu correo"
                        value="<%= locals.oldData ? oldData.email : null %>"/>
                        <% if (locals.errors && errors.email) { %>
                            <p class="text-danger"><%= errors.email.msg %></p>
                        <% } %>
                    </div>
                    <!-- Ingresar la contraseña -->
                    <div class="users-form__field">
                        <label class="users-form__label" for="password">Contraseña</label>
                        <input class="users-form__input" name="password" type="password" placeholder="Ingresá tu contraseña"/>
                        <% if (locals.errors && errors.password) { %>
                            <p class="text-danger"><%= errors.password.msg %></p>
                        <% } %>
                    </div>
                    <div class="users-form__field">
                        <label class="users-form__checkbox" for="checkbox">Recordar mi usuario</label>
                        <input class="users-form__checkbox" name="checkbox" type="checkbox">
                    </div>
                    <!-- Boton de iniciar sesion -->
                    <input type="submit" value="Iniciá Sesión" class="btnStyle2" />
                </form>
                <!-- usé un div aqui en el link para espaciarlo con el footer -->
                <div class="users__forgotten">
                    <!-- link de "olvidaste tu contraseña" -->
                    <a href="/users/userRecovery" class="users__forgotten-link">¿Olvidaste tu contraseña?</a>
                </div>
            </div>
        </section>
    </main>

    <!-- aca arranca el footer -->
    <%- include('../partials/footer') %>
  </body>
</html>
