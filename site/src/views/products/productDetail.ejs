<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partials/head') %>
        <title>Detalle del producto</title>
    </head>
<body>
    <%- include('../partials/header') %>
    <!-- Aca va el cuerpo la pagina -->
    <main>

        <section class="product-section">

            <div class="prod-detail">
                <figure class="prod-detail__slide">
                    <img src="<%= product.img %>" alt="<%= product.name %>">
                    <a href="#" class="prod-detail__prev">&#10094;</a>
                    <a href="#" class="prod-detail__next">&#10095;</a>
                </figure>
                <h2><%= product.name %></h2>
                <div class="flex-row">
                    <p class="prod-detail__price">$ <%= product.price - product.price * product.discount / 100 %> <span>$ <%= product.price %></span></p>
                    <button class="btn--comprar">Comprar</button>
                </div>
            </div>

            <div class="prod-desc">
                <p> <span class="bold">Descripción:</span>  <%= product.description %> </p>
                <p> <span class="bold">Nivel de cuidados:</span> <%= product.careLevel %></p>
                <p> <span class="bold">Categoría:</span> <%= product.type %></p>
                <div class="flex-row">
                    <div class="counter">
                        <span class="counter__minus">-</span>
                        <input class="counter__input" type="text" value="1"/>
                        <span class="counter__plus">+</span>
                    </div>
                    <button class="btn">Agrega al carrito</button>
                </div>
            </div>
        </section>

        <!-- seccion mas vendidos -->
        <h1 class="center-text">Mas vendidos</h2>
        <%- include('../partials/bestSells') %>
    </main>
    <!-- aca arranca el footer -->
    <%- include('../partials/footer') %>
    
</body>
</html>